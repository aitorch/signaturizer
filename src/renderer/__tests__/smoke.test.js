import { describe, it, expect } from 'vitest';

describe('Project scaffolding', () => {
  it('should have basic assertions working', () => {
    expect(1 + 1).toBe(2);
  });

  it('should have Math operations', () => {
    expect(Math.max(1, 2, 3)).toBe(3);
  });
});
